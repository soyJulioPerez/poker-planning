{
  "_comment": "Copiar a env.json (ignorado por git) y ajustar según tu entorno. Ver docs/sam-local-dynamodb-local.md para el detalle de por qué la region debe coincidir con la de tu perfil AWS (aws configure get region), y por qué DYNAMODB_ENDPOINT debe apuntar a la IP del contenedor de DynamoDB Local (docker inspect dynamodb-local --format '{{.NetworkSettings.Networks.bridge.IPAddress}}'), no a host.docker.internal.",
  "ConnectFunction": {
    "TABLE_NAME": "poker-planning-rooms",
    "DYNAMODB_ENDPOINT": "http://172.17.0.2:8000",
    "AWS_REGION": "us-east-2"
  },
  "DisconnectFunction": {
    "TABLE_NAME": "poker-planning-rooms",
    "DYNAMODB_ENDPOINT": "http://172.17.0.2:8000",
    "AWS_REGION": "us-east-2"
  }
}
